The .py file needs to be in the same folder as the .mp3 files

Fixes:
- Allow editing of year, genre, etc
- Allow user to select the order of the folders to be renamed, if they are not already named in alphabetical/numerical order

To install eyeD3:
$ tar xzf eyeD3-X.Y.Z.tar.gz
$ cd eyeD3-X.Y.Z
$ sudo python setup.py install
